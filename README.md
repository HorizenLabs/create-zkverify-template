# Create zkVerify Template

Clones `zkverifyjs-starter-template` on users machine.

## Deploy package

Ensure you login with an account that has zkVerify organisation permission on npm.

```shell
npm login
```

Publish

```shell
npm publish --access public
```
